# Template for bundles index.
# List all bundles (composition documents) here.

bundles:
  - id: "bundle.<product-or-domain>.<name>"
    path: "bundles/<bundle-file>.yaml"
    status: "<draft|active|retired>"
    environment: "PLACEHOLDER_ENVIRONMENT"

