# Authoring Guidelines

- Keep specs language- and technology-agnostic; defer tech decisions to
  profiles.
- Ensure `spec.yaml` remains the normative source and `spec.md` is a faithful
  narrative companion.

